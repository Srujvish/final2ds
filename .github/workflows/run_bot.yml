name: 20-Hour Algo Run

on:
  workflow_dispatch:  # Manual trigger only

jobs:
  run-algo:
    runs-on: ubuntu-latest
    timeout-minutes: 2000  # 20 hours max

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install ta yfinance requests pyotp pandas SmartApi logzero websocket-client smartapi-python datetime pycryptodome multitasking peewee

    - name: Run 20-Hour Algo Marathon
      env:
        API_KEY: ${{ secrets.API_KEY }}
        CLIENT_CODE: ${{ secrets.CLIENT_CODE }}
        PASSWORD: ${{ secrets.PASSWORD }}
        TOTP_SECRET: ${{ secrets.TOTP_SECRET }}
        BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
        CHAT_ID: ${{ secrets.CHAT_ID }}
      run: |
        echo "🏁 STARTING 20-HOUR ALGO at $(TZ='Asia/Kolkata' date)"
        echo "🕒 Current IST: $(TZ='Asia/Kolkata' date '+%H:%M:%S')"
        echo "⏰ Will run for 20 hours automatically"
        
        # Run for exactly 20 hours (72000 seconds)
        end_time=$(date -d "20 hours" +%s)
        
        while [ $(date +%s) -lt $end_time ]; do
          CURRENT_TIME_IST=$(TZ='Asia/Kolkata' date '+%H:%M:%S')
          echo "🔄 Running at: $CURRENT_TIME_IST IST"
          
          # Run one iteration
          python ultimate_algo.py
          
          # Wait 30 seconds before next iteration
          sleep 30
        done
        
        echo "✅ 20-HOUR RUN COMPLETED at $(TZ='Asia/Kolkata' date '+%H:%M:%S')"

    - name: Notify completion
      run: |
        echo "🎯 20-hour algo run completed at $(TZ='Asia/Kolkata' date)"
